# Engagements

Each ADM 1-cycle lives in its own folder here, named with a stable id like
`ENG-2026-001-omnichannel`. The `/togaf:init` slash command will scaffold a new
engagement folder and start the cycle.

Filename convention inside each engagement folder is `{phase}-{deliverable-id}.md`
so the platform can locate it from the engagement state.
